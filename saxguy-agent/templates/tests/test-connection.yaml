apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saxguy-agent.fullname" . }}-test-connection"
  labels:
    {{- include "saxguy-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "saxguy-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
